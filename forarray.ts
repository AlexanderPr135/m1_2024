const fruit = ["grusha", "yabloko", "persik", "ananas", "abrikos"]

for (let x=0; x<fruit.length; x++){
    if (fruit[x]==="yabloko"){
        console.log(`в данном массиве под номером ${x+1} есть фрукт ${fruit[x]}`)
    }
}

